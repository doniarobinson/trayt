export default function Calculator() {
  return (
    <div className="content__inner">
      <h1>Calculator</h1>
    </div>
  );
}

/*
              <Grid item xs={12}>
                <TextField
                  required
                  fullWidth
                  id="email"
                  label="Email Address"
                  name="email"
                  autoComplete="email"
                />
              </Grid>
              */
